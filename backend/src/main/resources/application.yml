spring:
  application:
    name: student-community-backend
  datasource:
    url: jdbc:mysql://localhost:3306/student_community?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: root
    password: yy256861
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false
  jackson:
    default-property-inclusion: non_null
  mail:
    host: smtp.gmail.com
    port: 587
    username: ulinkoffcial@gmail.com
    password: llvytbjwgjqqjqcm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  jwt-secret: change-me-in-production
  public-base-url: http://localhost:8080
  upload-root: uploads
  auth:
    email-code:
      from-address: ulinkoffcial@gmail.com
      expire-minutes: 10
      resend-interval-seconds: 60
  live-update:
    manifest-root: uploads/app-updates/live
